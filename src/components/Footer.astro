<footer class="footer">
    <div class="container">
        <div class="footer-grid">
            <div class="footer-info">
                <div class="footer-logo">
                    <span class="logo-fan">Fan</span>Blog
                </div>
                <p class="footer-tagline">Minimalist blog platform powered by Astro.</p>
            </div>
            <div class="footer-links">
                <a href="/">Home</a>
                <a href="/blog">Blog</a>
                <a href="/admin">Admin</a>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; {new Date().getFullYear()} FanBlog. Designed with precision by <a href="https://irfan-syarifudin.vercel.app/" target="_blank" rel="noopener noreferrer" class="author-link">Irfan Syarifudin</a>.</p>
        </div>
    </div>
</footer>

<style>
    .footer {
        padding: 5rem 0 3rem;
        border-top: 1px solid var(--border-color);
        background-color: var(--bg-color);
    }

    .footer-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 3rem;
    }

    .footer-logo {
        font-weight: 800;
        font-size: 1.5rem;
        letter-spacing: -0.05em;
        margin-bottom: 0.5rem;
        color: var(--text-color);
    }

    .logo-fan {
        color: var(--color-primary);
    }

    .footer-tagline {
        color: var(--text-muted);
        font-size: 0.95rem;
        max-width: 300px;
    }

    .footer-links {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        align-items: flex-end;
    }

    .footer-links a {
        color: var(--text-muted);
        font-size: 0.9rem;
        font-weight: 500;
    }

    .footer-links a:hover {
        color: var(--color-primary);
    }

    .footer-bottom {
        padding-top: 2rem;
        border-top: 1px solid var(--border-color);
        display: flex;
        justify-content: center;
        color: var(--text-muted);
        font-size: 0.85rem;
    }

    .author-link {
        color: var(--text-color);
        font-weight: 600;
        text-decoration: none;
        position: relative;
    }

    .author-link::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 100%;
        height: 1px;
        background-color: var(--color-primary);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }

    .author-link:hover {
        color: var(--color-primary);
    }

    .author-link:hover::after {
        transform: scaleX(1);
    }

    @media (max-width: 768px) {
        .footer-grid {
            grid-template-columns: 1fr;
            text-align: center;
        }
        .footer-info {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .footer-links {
            align-items: center;
        }
    }
</style>
